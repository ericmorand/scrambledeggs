import * as React from "react";

import type {FunctionComponent} from "react";

import {<%= camelName %>, <%= camelName %>Properties} from "<%= relativePathToComponent %>";

export const <%= camelName %>Test: FunctionComponent<<%= camelName %>Properties> = ({content}) => {
    return <<%= camelName %> content={content}/>;
};